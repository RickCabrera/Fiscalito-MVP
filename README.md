# Fiscalito MVP
Monorepo con el frontend (apps/store) y backend (apps/api) de Fiscalito.

## Setup
- Frontend: `cd apps/store && npm install && npm run dev`
- Backend: `cd apps/api && pip install -e . && uvicorn app.main:app --reload`
